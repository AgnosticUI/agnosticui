# AgnosticUI Vue Playground

Ensure that inside the local AgnosticUI library you've done `npm link` then you can simply do:

```shell
    "relink": "npm link agnosticui-core",
    "storybook": "npm run relink && storybook dev -p 6006",
```

So:
`npm run storybook`
