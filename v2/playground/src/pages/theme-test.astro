---
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>Theme Test - AgnosticUI v2</title>

    <!-- Import our semantic tokens and dark theme overrides -->
    <style>
      /* Semantic tokens (base) */
      :root {
        --ag-space-0: 0;
        --ag-space-1: 0.25rem;
        --ag-space-2: 0.5rem;
        --ag-space-3: 0.75rem;
        --ag-space-4: 1rem;
        --ag-space-5: 1.25rem;
        --ag-space-6: 1.5rem;
        --ag-space-8: 2rem;
        --ag-radius-none: 0;
        --ag-radius-sm: 0.25rem;
        --ag-radius-md: 0.375rem;
        --ag-radius-lg: 1rem;
        --ag-radius-full: 9999px;
        --ag-motion-fast: 0.15s;
        --ag-motion-medium: 0.2s;
        --ag-motion-slow: 0.4s;
        --ag-z-index-dropdown: 100;
        --ag-z-index-modal: 1000;
        --ag-z-index-toast: 1100;
        --ag-focus-width: 2px;
        --ag-focus-offset: 2px;
        --ag-text-primary: #111827;
        --ag-text-secondary: #6b7280;
        --ag-text-muted: #9ca3af;
        --ag-background-primary: #ffffff;
        --ag-background-secondary: #f9fafb;
        --ag-background-tertiary: #f3f4f6;
        --ag-border-primary: #e5e7eb;
        --ag-border-secondary: #d1d5db;
        --ag-primary: #077acb;
        --ag-primary-hover: #063f69;
        --ag-danger: #dc2626;
        --ag-danger-hover: #b91c1c;
        --ag-warning: #d97706;
        --ag-warning-hover: #b45309;
        --ag-focus: #2563eb;
        --ag-on-primary: #ffffff;
        --ag-on-danger: #ffffff;
        --ag-on-warning: #ffffff;
      }

      /* Dark theme overrides */
      [data-theme="dark"] {
        --ag-text-primary: #f9fafb;
        --ag-text-secondary: #d1d5db;
        --ag-text-muted: #9ca3af;
        --ag-background-primary: #111827;
        --ag-background-secondary: #1f2937;
        --ag-background-tertiary: #374151;
        --ag-border-primary: #374151;
        --ag-border-secondary: #4b5563;
        --ag-focus: #3b82f6;
      }
    </style>

    <style>
      /* Test styles using our semantic tokens */
      .theme-test-container {
        padding: var(--ag-space-6);
        background: var(--ag-background-primary);
        color: var(--ag-text-primary);
        border: 1px solid var(--ag-border-primary);
        border-radius: var(--ag-radius-lg);
        margin: var(--ag-space-4);
        min-height: 500px;
        transition: background var(--ag-motion-medium) ease,
                    color var(--ag-motion-medium) ease,
                    border-color var(--ag-motion-medium) ease;
      }

      .theme-test-header {
        color: var(--ag-text-primary);
        margin-bottom: var(--ag-space-4);
      }

      .theme-test-section {
        background: var(--ag-background-secondary);
        padding: var(--ag-space-4);
        border-radius: var(--ag-radius-md);
        margin-bottom: var(--ag-space-4);
        border: 1px solid var(--ag-border-secondary);
      }

      .theme-test-text-secondary {
        color: var(--ag-text-secondary);
        margin-bottom: var(--ag-space-2);
      }

      .theme-test-text-muted {
        color: var(--ag-text-muted);
        font-size: 0.875rem;
      }

      .theme-toggle-button {
        background: var(--ag-primary);
        color: var(--ag-on-primary);
        border: none;
        padding: var(--ag-space-3) var(--ag-space-4);
        border-radius: var(--ag-radius-md);
        cursor: pointer;
        font-weight: 600;
        margin-right: var(--ag-space-2);
        transition: background var(--ag-motion-medium) ease;
      }

      .theme-toggle-button:hover {
        background: var(--ag-primary-hover);
      }

      .theme-toggle-button:focus {
        outline: var(--ag-focus-width) solid var(--ag-focus);
        outline-offset: var(--ag-focus-offset);
      }

      .danger-button {
        background: var(--ag-danger);
        color: var(--ag-on-danger);
        border: none;
        padding: var(--ag-space-3) var(--ag-space-4);
        border-radius: var(--ag-radius-md);
        cursor: pointer;
        font-weight: 600;
        margin-right: var(--ag-space-2);
        transition: background var(--ag-motion-medium) ease;
      }

      .danger-button:hover {
        background: var(--ag-danger-hover);
      }

      .danger-button:focus {
        outline: var(--ag-focus-width) solid var(--ag-focus);
        outline-offset: var(--ag-focus-offset);
      }

      .warning-button {
        background: var(--ag-warning);
        color: var(--ag-on-warning);
        border: none;
        padding: var(--ag-space-3) var(--ag-space-4);
        border-radius: var(--ag-radius-md);
        cursor: pointer;
        font-weight: 600;
        transition: background var(--ag-motion-medium) ease;
      }

      .warning-button:hover {
        background: var(--ag-warning-hover);
      }

      .warning-button:focus {
        outline: var(--ag-focus-width) solid var(--ag-focus);
        outline-offset: var(--ag-focus-offset);
      }

      .current-theme {
        background: var(--ag-background-tertiary);
        padding: var(--ag-space-2) var(--ag-space-3);
        border-radius: var(--ag-radius-sm);
        display: inline-block;
        margin-left: var(--ag-space-2);
        font-size: 0.75rem;
        font-weight: 600;
        color: var(--ag-text-secondary);
      }
    </style>
  </head>
  <body>
    <div class="theme-test-container">
      <div class="theme-test-header">
        <h1>🎨 Theme System Test</h1>
        <p>Test our semantic design token system with light/dark theme switching.</p>

        <div style="margin: var(--ag-space-4) 0;">
          <button class="theme-toggle-button" onclick="toggleTheme()">
            Toggle Theme
          </button>
          <span class="current-theme" id="current-theme">Light Theme</span>
        </div>
      </div>

      <div class="theme-test-section">
        <h2>Brand Colors (Work Across Themes)</h2>
        <p class="theme-test-text-secondary">
          These buttons use shared brand colors with white text - they should work well on both light and dark themes.
        </p>

        <button class="theme-toggle-button">Primary Button</button>
        <button class="danger-button">Danger Button</button>
        <button class="warning-button">Warning Button</button>
      </div>

      <div class="theme-test-section">
        <h2>Semantic Tokens</h2>
        <p class="theme-test-text-secondary">
          This section uses semantic tokens that change based on the theme.
        </p>
        <div class="theme-test-text-muted">
          • Text colors: --ag-text-primary, --ag-text-secondary, --ag-text-muted
        </div>
        <div class="theme-test-text-muted">
          • Background colors: --ag-background-primary, --ag-background-secondary, --ag-background-tertiary
        </div>
        <div class="theme-test-text-muted">
          • Border colors: --ag-border-primary, --ag-border-secondary
        </div>
        <div class="theme-test-text-muted">
          • Focus color: --ag-focus (changes per theme for optimal contrast)
        </div>
      </div>

      <div class="theme-test-section">
        <h2>Token Usage Example</h2>
        <pre style="background: var(--ag-background-tertiary); padding: var(--ag-space-3); border-radius: var(--ag-radius-sm); overflow-x: auto; font-size: 0.875rem; color: var(--ag-text-primary);">/* Component styles using semantic tokens */
.my-component {
  background: var(--ag-background-primary);
  color: var(--ag-text-primary);
  border: 1px solid var(--ag-border-primary);
  padding: var(--ag-space-4);
  border-radius: var(--ag-radius-md);
}

.my-button {
  background: var(--ag-primary);
  color: var(--ag-on-primary);
  padding: var(--ag-space-3) var(--ag-space-4);
}

.my-button:focus {
  outline: var(--ag-focus-width) solid var(--ag-focus);
  outline-offset: var(--ag-focus-offset);
}</pre>
      </div>
    </div>

    <script>
      function toggleTheme() {
        const html = document.documentElement;
        const currentTheme = html.getAttribute('data-theme');
        const currentThemeSpan = document.getElementById('current-theme');

        if (currentTheme === 'dark') {
          html.removeAttribute('data-theme');
          currentThemeSpan.textContent = 'Light Theme';
          console.log('🌞 Switched to light theme');
        } else {
          html.setAttribute('data-theme', 'dark');
          currentThemeSpan.textContent = 'Dark Theme';
          console.log('🌙 Switched to dark theme');
        }
      }

      // Initialize theme display
      document.addEventListener('DOMContentLoaded', () => {
        const html = document.documentElement;
        const currentThemeSpan = document.getElementById('current-theme');
        const currentTheme = html.getAttribute('data-theme');

        if (currentTheme === 'dark') {
          currentThemeSpan.textContent = 'Dark Theme';
        } else {
          currentThemeSpan.textContent = 'Light Theme';
        }

        console.log('🎨 Theme test page loaded. Click "Toggle Theme" to test switching!');
      });
    </script>
  </body>
</html>