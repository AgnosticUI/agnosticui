---
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>Button + Theme Test - AgnosticUI v2</title>

    <!-- Import our semantic tokens and dark theme overrides -->
    <style>
      /* Semantic tokens (base) */
      :root {
        --ag-space-0: 0;
        --ag-space-1: 0.25rem;
        --ag-space-2: 0.5rem;
        --ag-space-3: 0.75rem;
        --ag-space-4: 1rem;
        --ag-space-5: 1.25rem;
        --ag-space-6: 1.5rem;
        --ag-space-8: 2rem;
        --ag-radius-none: 0;
        --ag-radius-sm: 0.25rem;
        --ag-radius-md: 0.375rem;
        --ag-radius-lg: 1rem;
        --ag-radius-full: 9999px;
        --ag-motion-fast: 0.15s;
        --ag-motion-medium: 0.2s;
        --ag-motion-slow: 0.4s;
        --ag-z-index-dropdown: 100;
        --ag-z-index-modal: 1000;
        --ag-z-index-toast: 1100;
        --ag-focus-width: 2px;
        --ag-focus-offset: 2px;
        --ag-text-primary: #111827;
        --ag-text-secondary: #6b7280;
        --ag-text-muted: #9ca3af;
        --ag-background-primary: #ffffff;
        --ag-background-secondary: #f9fafb;
        --ag-background-tertiary: #f3f4f6;
        --ag-border-primary: #e5e7eb;
        --ag-border-secondary: #d1d5db;
        --ag-primary: #077acb;
        --ag-primary-hover: #063f69;
        --ag-danger: #dc2626;
        --ag-danger-hover: #b91c1c;
        --ag-warning: #d97706;
        --ag-warning-hover: #b45309;
        --ag-focus: #2563eb;
        --ag-on-primary: #ffffff;
        --ag-on-danger: #ffffff;
        --ag-on-warning: #ffffff;
      }

      /* Dark theme overrides */
      [data-theme="dark"] {
        --ag-text-primary: #f9fafb;
        --ag-text-secondary: #d1d5db;
        --ag-text-muted: #9ca3af;
        --ag-background-primary: #111827;
        --ag-background-secondary: #1f2937;
        --ag-background-tertiary: #374151;
        --ag-border-primary: #374151;
        --ag-border-secondary: #4b5563;
        --ag-focus: #3b82f6;
      }
    </style>

    <!-- Button component styling using semantic tokens -->
    <style>
      .test-container {
        padding: var(--ag-space-6);
        background: var(--ag-background-primary);
        color: var(--ag-text-primary);
        border: 1px solid var(--ag-border-primary);
        border-radius: var(--ag-radius-lg);
        margin: var(--ag-space-4);
        min-height: 500px;
        transition: background var(--ag-motion-medium) ease,
                    color var(--ag-motion-medium) ease,
                    border-color var(--ag-motion-medium) ease;
      }

      .test-header {
        color: var(--ag-text-primary);
        margin-bottom: var(--ag-space-4);
      }

      .test-section {
        background: var(--ag-background-secondary);
        padding: var(--ag-space-4);
        border-radius: var(--ag-radius-md);
        margin-bottom: var(--ag-space-4);
        border: 1px solid var(--ag-border-secondary);
      }

      .button-group {
        display: flex;
        flex-wrap: wrap;
        gap: var(--ag-space-3);
        margin: var(--ag-space-4) 0;
        align-items: center;
      }

      .theme-toggle-button {
        background: var(--ag-primary);
        color: var(--ag-on-primary);
        border: none;
        padding: var(--ag-space-3) var(--ag-space-4);
        border-radius: var(--ag-radius-md);
        cursor: pointer;
        font-weight: 600;
        margin-right: var(--ag-space-2);
        transition: background var(--ag-motion-medium) ease;
      }

      .theme-toggle-button:hover {
        background: var(--ag-primary-hover);
      }

      .theme-toggle-button:focus {
        outline: var(--ag-focus-width) solid var(--ag-focus);
        outline-offset: var(--ag-focus-offset);
      }

      .current-theme {
        background: var(--ag-background-tertiary);
        padding: var(--ag-space-2) var(--ag-space-3);
        border-radius: var(--ag-radius-sm);
        display: inline-block;
        margin-left: var(--ag-space-2);
        font-size: 0.75rem;
        font-weight: 600;
        color: var(--ag-text-secondary);
      }

      /* Basic Button styling using our semantic tokens */
      ag-button::part(button) {
        background: var(--ag-background-secondary);
        border: 1px solid var(--ag-border-primary);
        color: var(--ag-text-primary);
        padding: var(--ag-space-3) var(--ag-space-4);
        border-radius: var(--ag-radius-md);
        font-weight: 500;
        transition: all var(--ag-motion-medium) ease;
      }

      ag-button::part(button):hover:not(:disabled) {
        background: var(--ag-background-tertiary);
        border-color: var(--ag-primary);
      }

      /* Primary variant */
      ag-button[variant="primary"]::part(button) {
        background: var(--ag-primary);
        color: var(--ag-on-primary);
        border-color: var(--ag-primary);
      }

      ag-button[variant="primary"]::part(button):hover:not(:disabled) {
        background: var(--ag-primary-hover);
        border-color: var(--ag-primary-hover);
      }

      /* Danger variant */
      ag-button[variant="danger"]::part(button) {
        background: var(--ag-danger);
        color: var(--ag-on-danger);
        border-color: var(--ag-danger);
      }

      ag-button[variant="danger"]::part(button):hover:not(:disabled) {
        background: var(--ag-danger-hover);
        border-color: var(--ag-danger-hover);
      }

      /* Warning variant */
      ag-button[variant="warning"]::part(button) {
        background: var(--ag-warning);
        color: var(--ag-on-warning);
        border-color: var(--ag-warning);
      }

      ag-button[variant="warning"]::part(button):hover:not(:disabled) {
        background: var(--ag-warning-hover);
        border-color: var(--ag-warning-hover);
      }

      /* Size variants */
      ag-button[size="sm"]::part(button) {
        padding: var(--ag-space-2) var(--ag-space-3);
        font-size: 0.875rem;
      }

      ag-button[size="lg"]::part(button) {
        padding: var(--ag-space-4) var(--ag-space-6);
        font-size: 1.125rem;
      }

      .note {
        color: var(--ag-text-muted);
        font-size: 0.875rem;
        margin-top: var(--ag-space-2);
      }
    </style>
  </head>
  <body>
    <div class="test-container">
      <div class="test-header">
        <h1>🚀 Button + Theme Integration Test</h1>
        <p>Test Button component using semantic tokens with light/dark theme switching.</p>

        <div style="margin: var(--ag-space-4) 0;">
          <button class="theme-toggle-button" onclick="toggleTheme()">
            Toggle Theme
          </button>
          <span class="current-theme" id="current-theme">Light Theme</span>
        </div>
      </div>

      <div class="test-section">
        <h2>Button Variants with Semantic Tokens</h2>
        <p class="note">
          These buttons use semantic tokens that automatically adapt to light/dark themes.
        </p>

        <div class="button-group">
          <ag-button>Default Button</ag-button>
          <ag-button variant="primary">Primary Button</ag-button>
          <ag-button variant="danger">Danger Button</ag-button>
          <ag-button variant="warning">Warning Button</ag-button>
        </div>

        <p class="note">
          Notice how buttons adapt their appearance when you toggle themes. Brand colors (primary, danger, warning) stay consistent while text and borders adapt to the theme.
        </p>
      </div>

      <div class="test-section">
        <h2>Button States</h2>
        <div class="button-group">
          <ag-button disabled>Disabled Button</ag-button>
          <ag-button variant="primary" disabled>Disabled Primary</ag-button>
          <ag-button loading>Loading Button</ag-button>
        </div>
      </div>

      <div class="test-section">
        <h2>Focus Ring Test</h2>
        <p class="note">
          Tab through these buttons to test focus rings. Notice how the focus color automatically adjusts for optimal contrast in each theme.
        </p>
        <div class="button-group">
          <ag-button>Tab to me</ag-button>
          <ag-button variant="primary">Then here</ag-button>
          <ag-button variant="danger">And here</ag-button>
          <ag-button toggle>Toggle me</ag-button>
        </div>
      </div>

      <div class="test-section">
        <h2>Size Variants</h2>
        <div class="button-group">
          <ag-button size="sm" variant="primary">Small</ag-button>
          <ag-button variant="primary">Medium (default)</ag-button>
          <ag-button size="lg" variant="primary">Large</ag-button>
        </div>
      </div>
    </div>

    <script>
      // Import the Button component
      if (typeof window !== "undefined") {
        import("agnosticui-core");
      }

      // Make toggleTheme globally available
      window.toggleTheme = function() {
        const html = document.documentElement;
        const currentTheme = html.getAttribute('data-theme');
        const currentThemeSpan = document.getElementById('current-theme');

        if (currentTheme === 'dark') {
          html.removeAttribute('data-theme');
          currentThemeSpan.textContent = 'Light Theme';
          console.log('🌞 Switched to light theme');
        } else {
          html.setAttribute('data-theme', 'dark');
          currentThemeSpan.textContent = 'Dark Theme';
          console.log('🌙 Switched to dark theme');
        }
      };

      // Initialize theme display
      document.addEventListener('DOMContentLoaded', () => {
        const html = document.documentElement;
        const currentThemeSpan = document.getElementById('current-theme');
        const currentTheme = html.getAttribute('data-theme');

        if (currentTheme === 'dark') {
          currentThemeSpan.textContent = 'Dark Theme';
        } else {
          currentThemeSpan.textContent = 'Light Theme';
        }

        console.log('🎨 Button + Theme test page loaded. Click "Toggle Theme" to test switching!');
      });
    </script>
  </body>
</html>