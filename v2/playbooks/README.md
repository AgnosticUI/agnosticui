# AgnosticUI AI Playbooks

This directory contains **AI Playbooks** designed to help developers and AI agents build complex interfaces using AgnosticUI primitives. Each playbook is a collection of architectural patterns and specialized prompt engineering instructions.

## Generative UI Strategy

Unlike static templates, these playbooks center around a `PROMPT.md` file. These files provide LLMs (Large Language Models) with the precise semantic context, component APIs, and design tokens required to generate high-quality, framework-agnostic code.

## Available AI Playbooks

| Playbook                  | Components                                                                                                                                                    | Narrative                 |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- |
| [Login](login/)           | Button, Card, Checkbox, Divider, Image, Input, Link                                                                                                           | User signs in             |
| [Onboarding](onboarding/) | Alert, Button, Icon, SelectionButton, SelectionCard, Timeline                                                                                                 | New user setup wizard     |
| [Dashboard](dashboard/)   | Accordion, Alert, Avatar, Badge, Breadcrumb, Button, Card, Divider, EmptyState, Flex, Header, IconButton, Progress, Sidebar, SkeletonLoader, Tabs, Tag, Toast | User's home screen        |
| [Support](support/)       | Button, Combobox, Dialog, Drawer, Kbd, Menu, MessageBubble, Pagination, Popover, Radio, Rating, Select, Slider, Tabs, Toggle, Tooltip                         | User needs help           |
| [Data Grid](grid/)        | Button, Checkbox, EmptyState, Flex, Icon, IconButton, Input, Pagination, Select, Spinner, Tag                                                                 | Complex data manipulation |

## How to Use with AI Agents

1.  **Context Loading**: Direct your AI agent (e.g., Cursor, Claude, Gemini, Windsurf, or other LLM) to read the relevant `PROMPT*.md` file in the playbook's directory.
2.  **Generation**: Ask the agent to "Apply the AgnosticUI [Playbook Name] patterns to my specific feature."
3.  **Refinement**: Use the architectural guidelines within each playbook to ensure the generated code maintains theme consistency and follows AgnosticUI's "Local-First" philosophy.

## Directory Structure

```
v2/playbooks/
├── README.md                  # This file
├── CREATING-VARIANTS.md       # Guide for deriving new design variants from existing playbooks
├── login/                     # Login form playbook
├── onboarding/                # Onboarding wizard playbook
├── dashboard/                 # Discovery dashboard playbook
└── support/                   # Support center playbook
    │
    │ # PROMPTS (input — run these with Claude or another LLM)
    ├── PROMPT-3-FRAMEWORKS.md # Builds all 3 frameworks → react-example/, vue-example/, lit-example/
    ├── PROMPT-REACT.md        # Builds React only → react/
    ├── PROMPT-VUE.md          # Builds Vue only → vue/
    ├── PROMPT-LIT.md          # Builds Lit only → lit/
    │
    │ # DESIGN ASSETS (shared resources referenced by prompts)
    ├── design/                # Mockups, logos, icons (varies per playbook)
    │
    │ # OUTPUTS (generated by running prompts with Claude)
    ├── react/                 # Output from PROMPT-REACT.md
    ├── vue/                   # Output from PROMPT-VUE.md
    ├── lit/                   # Output from PROMPT-LIT.md
    │
    ├── react-example/         # Output from PROMPT-3-FRAMEWORKS.md
    ├── vue-example/           # Output from PROMPT-3-FRAMEWORKS.md
    ├── lit-example/           # Output from PROMPT-3-FRAMEWORKS.md
    │
    │ # VARIANT OUTPUTS (generated by variant prompts, e.g. PROMPT-3-FRAMEWORKS-V2.md)
    ├── react-example-v2/      # Variant II output
    ├── vue-example-v2/        # Variant II output
    └── lit-example-v2/        # Variant II output
```

## How It Works

Playbooks use a **prompt-driven approach**:

1. **Prompts are the source of truth** - Each `PROMPT-*.md` file contains complete instructions for Claude to build the example
2. **Outputs are generated** - The framework directories are created by running prompts with Claude
3. **Design assets are shared** - The `design/` directory contains mockups and images that prompts reference and copy into builds

### Single-Framework vs 3-Frameworks

There are two types of examples:

**Single-Framework** (`PROMPT-REACT.md`, `PROMPT-VUE.md`, `PROMPT-LIT.md`):

- Use Playfair Display font
- Apple + Google social buttons
- `logo.svg` and `login-bg.jpg` assets
- Output to `react/`, `vue/`, `lit/`

**3-Frameworks** (`PROMPT-3-FRAMEWORKS.md`):

- Use Merriweather font
- Facebook + Google social buttons
- `logo-3-frameworks.svg` and `login-bg-3-frameworks.jpg` assets
- Output to `react-example/`, `vue-example/`, `lit-example/`

### Building a Single Framework

To build just one framework, run the corresponding prompt with Claude:

```bash
# In the playbook directory, start Claude and reference the prompt
cd v2/playbooks/login
claude # or LLM of choice

# Then ask Claude to follow the prompt
> Please follow the instructions in PROMPT-REACT.md
```

This generates the example in `react/`.

### Building All Frameworks

To build all three frameworks at once:

```bash
cd v2/playbooks/login
claude # or LLM of choice

> Please follow the instructions in PROMPT-3-FRAMEWORKS.md
```

This generates examples in `react-example/`, `vue-example/`, and `lit-example/`.

## StackBlitz Integration

Playbooks are available as live StackBlitz demos on the documentation site. The integration works via the `PlaybookStackBlitz` Vue component which fetches files from GitHub and creates a StackBlitz project on-the-fly.

### Key Files

- **Component:** `v2/site/docs/.vitepress/theme/components/PlaybookStackBlitz.vue`
- **Config:** `v2/site/docs/.vitepress/theme/components/playbooks-config.ts`

### How It Works

1. User clicks "Open in StackBlitz" button on the docs site
2. Component reads the playbook config from `playbooks-config.ts`
3. Files are fetched from `raw.githubusercontent.com`
4. Binary file references (like `.jpg`) are replaced with raw GitHub URLs
5. StackBlitz SDK creates and opens the project

## Creating a New Playbook

### 1. Create the Directory Structure

```bash
mkdir -p v2/playbooks/[name]/design
```

### 2. Add Design Assets

Place shared assets in the `design/` directory:

**Required:**

- Design mockups (Desktop.png, Tablet.png, Phone.png, etc.)

**Optional (varies by playbook):**

- Icons (e.g., `google-icon.svg`, `apple-icon.svg`)
- Logos (e.g., `logo.svg`, `logo-3-frameworks.svg`)
- Background images (e.g., `login-bg.jpg`)

Not all playbooks require icons, logos, or background images. For example, the `onboarding` playbook only uses mockups, while `login` includes social login icons and background images.

### 3. Create the Prompts

Create prompt files that instruct Claude how to build the example:

- `PROMPT-3-FRAMEWORKS.md` - Instructions to build all frameworks into `react-example/`, `vue-example/`, `lit-example/`
- `PROMPT-REACT.md` - Instructions to build React into `react/`
- `PROMPT-VUE.md` - Instructions to build Vue into `vue/`
- `PROMPT-LIT.md` - Instructions to build Lit into `lit/`

Each prompt should include:

- Project setup instructions (Vite, dependencies)
- AgnosticUI CLI initialization
- Component implementation details
- Asset copying instructions
- Responsive layout specifications

See `login/PROMPT-*.md` for reference.

### 4. Update the StackBlitz Config

Edit `v2/site/docs/.vitepress/theme/components/playbooks-config.ts` to include the new playbook.

### 5. Create the Documentation Page

Add a markdown file at `v2/site/docs/playbooks/[name].md` with the PlaybookStackBlitz component.

### 6. Add to Navigation

Update `v2/site/docs/.vitepress/config.mts` to include the new playbook in the sidebar.

## Binary Files

StackBlitz SDK only supports text files. For binary assets (images, fonts):

1. Keep them in the `design/` folder of the playbook
2. Prompts copy them to each framework's `public/` folder during build
3. Add URL mappings to `binaryFiles` in the StackBlitz config

## Reference Implementation

See the `login/` playbook for a complete example of the prompt-driven approach.
