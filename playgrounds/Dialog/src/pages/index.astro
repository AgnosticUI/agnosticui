---
// Style Imports
import '../styles/global.css';

// common.min.css is distributed with agnostic-react
// and agnostic-svelte too. But, since we're using all
// of these together, we only need to import global CSS
// once and in one place.
// common.min.css combines properties, resets, utilities
// import "agnostic-vue/dist/common.min.css";
// But!! Rob much prefers individual modular import ;)
import "agnostic-vue/dist/common.properties.min.css";
import "agnostic-vue/dist/common.resets.min.css";
import "agnostic-vue/dist/common.utilities.min.css";
import "agnostic-vue/dist/opinions.min.css";

// Component Imports
import Heading from '../components/Heading.astro';
import React from '../components/React.jsx';
import Svelte from '../components/Svelte.svelte';
import Vue from '../components/Vue.vue';

// Component Script:
// You can write any JavaScript/TypeScript that you'd like here.
// It will run during the build, but never in the browser.
// All variables are available to use in the HTML template below.
let title = 'Astro/AgnosticUI Site';

// Full Astro Component Syntax:
// https://docs.astro.build/core-concepts/astro-components/
---
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width">
	<title>{title}</title>

	<link rel="icon" type="image/x-icon" href="/favicon.ico" />

	<style>
		header {
			display: flex;
			flex-direction: column;
			gap: 1em;
			max-width: min(100%, 68ch);
		}
	</style>
</head>
<body>
	<main>
		<Heading />
    <div class="frameworks">
      <!-- If you swap in the version that's SSR (not client hydrated) you can see the
      implications on the "I have read and agree to the Terms of Service" UX — no longer
      does checking the checkbox enable the button. Good example of when client hydration
      is a requirement which is basically anytime you need dynamic functionality. This of
      course happens for all the frameworks here not just React ;-)  -->
		  <!-- <React /> -->
      <React client:load />
		  <Vue client:only />
		  <Svelte client:load />
    </div>
	</main>
  <div id="portal-root"></div>
</body>
</html>
