---
export function getStaticPaths() {
  return [
    // Generates: /en-v1/buttons
    {params: {'component': 'buttons'}},
    // Generates: /en-v1/foobar
    {params: {'component': 'foobar'}},
  ];
}
---
